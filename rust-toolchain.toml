# Pinned so local runs and CI use the same compiler, clippy and rustfmt.
# Bump deliberately; `rust-version` in Cargo.toml tracks the same release.
[toolchain]
channel = "1.98.1"
components = ["clippy", "rustfmt"]
