# The nightly's shards (D-064): every test of ananke-sim's integration binaries, sim/tests/*.rs,
# assigned to one of six shards, each a job of its own. The weight is the test's CPU seconds
# (user + sys) at ANANKE_SEEDS=1000 and ANANKE_DEEP_SEEDS=100, run alone in release, measured on
# fc75f68 on an 8-core Apple machine; the assignment is longest-first into the lightest shard, so
# the shards carry equal weight. Everything outside these binaries runs in the `rest` job.
# scripts/check-nightly-shards.sh, part of the gate, fails when a test here does not exist, is
# listed twice, or when a test of these binaries is in no shard: a new sweep names its shard here
# in the commit that adds it.
# Rows added since fc75f68 are weighed the same way on the tree that adds them and placed
# longest-first into the lightest shard; the rows before them keep fc75f68's weights. D-068's
# four were weighed on its tree, on an idle machine, on their high-rate share. D-069 renamed
# seed 158's pin and added seed 102's, both weighed on its tree; the renamed row keeps its
# shard and the new one went to the lightest, which was the same shard 6.
# D-078 added one row, weighed on its tree at ANANKE_SEEDS=1000 ANANKE_DEEP_SEEDS=100 in
# release, alone in the command but with other agents' slices building on the same laptop
# (load average 69.71/57.02/39.49): 48.20 user + 2.46 sys = 50.7 cpu s. Shards 5 and 6 were
# the lightest at 1242.0 and it went to the lower-numbered of the two.
# D-078's review added a second row, the follower-log bound's pinned pair. It is pinned at
# one seed, not swept, so the tier does not move it: weighed from the built test binary
# (`raft-<hash> <test> --exact`) rather than through cargo, so the figure is the test's own
# cost and not a build's, at 0.415 user + 0.033 sys = 0.4 cpu s, load 50.28/59.22/56.19.
# Shard 6 was the lightest at 1242.0 and takes it, to 1242.4.
#
# D-076's eight rows — the node scenario, `ranges` — were weighed on its own tree on an Apple
# M2 of 8 cores, on AC; the first seven under load average 149 from the other Stage B slices
# building beside it, so their cpu_s are if anything high, and the eighth (the directed scenario
# the review of D-076 added) on an idle machine at 0.05 cpu s, rounded up to 0.1 like the cheap
# rows beside it. Re-weighed after the review's fixes, on an idle machine, the two sweeps came
# to 60.7 and 51.9 cpu s against the 59.0 and 53.9 recorded here: within the noise of the load
# they were first measured under, so the rows keep their figures.
# D-077 added two rows, both `ranges` and both cheap. The first, the directed re-seed scenario,
# was weighed on its tree on the same Apple M2, on AC under load average 6.1, at 0.01 cpu s on
# the built binary (it runs one 600 ms scenario five times and no sweep). The second, the
# restart its review added, was weighed the same way from the built release binary at
# ANANKE_SEEDS=1000 ANANKE_DEEP_SEEDS=100: user and sys were each below the timer's 0.01 s
# resolution on three runs (real 0.41 s, then 0.00 s twice, the first being the binary's
# page-in), under load average 92.8 from the slices building beside it, which can only make a
# figure high. Both are rounded up to 0.1 like the cheap rows beside them, and longest-first
# into the lightest shard put both in shard 2. D-077 also left the shard 2 total reading 1276.9
# against rows summing to 1277.0.
# D-076's rows, D-077's and D-078's were placed against three different tables, on branches
# open at the same time. **This merge combined two independent placements**: it takes the union
# of both sides' rows and keeps every row in the shard its own side placed it in, re-placing
# none of them, and every total below is recomputed from the rows that resulted rather than
# carried over from either side. The merged shards run 1277.1 to 1351.7 cpu s, a spread of 75
# on about 1 300, so the night is bounded by shard 5 at about 4 % over the ideal. The next row
# added goes into the lightest, which is shard 2.
#
# D-080 added one row, the pinned pair of seeds whose histories the old search could not
# decide. It is pinned at two seeds, not swept, so the tier does not move it: weighed from
# the built release test binary rather than through cargo, at 0.3 cpu s. Shard 6 takes it.
# D-082's seventeen rows — the raft sweep's arms on the node, `node` — were weighed on
# its own tree at ANANKE_SEEDS=1000 in release, from the
# built binary (`node-<hash> <test> --exact`) rather than through cargo, so each figure
# is the test's own cost and not a build's. On an Apple M2 of 8 cores, on AC Power, no
# thermal warning, with other agents' slices building beside it: load averages
# 53.69/47.43/54.61 before and 80.57/65.30/60.65 after, which is why these are cpu
# seconds (user + sys) and not wall time (D-070). They total 446.31 cpu s.
# Issue #102's discipline applies to the mutation figures these rows sit beside, and it
# is not what it first looked like: incremental compilation is not the cause, and five
# distinct binaries of one tree give byte-identical traces. What bites is a mutation
# harness that restores a file with an mtime *older* than the build that compiled the
# mutant — cargo then calls the unit fresh and the tree goes on running the mutant. Back
# up by copy, touch on restore, and confirm the next build recompiled.
# Six of the seventeen are the variants the node has no path for yet, one test each so
# that `cargo test --list` and this table carry the debt rather than a paragraph of
# prose; each runs its variant over `high_rate_share()` and asserts the path absent.
# Six more run that share and are tens of cpu seconds for the same reason; the two
# large rows are the correct node on every seed and `LeaseTrustsTheClock`, whose catch
# is asserted from the thousand-seed tier and which therefore runs every seed.
# Placed longest-first into the lightest shard, against the table as this branch found
# it, where the shards then ran 1328.0 to 1442.7 cpu s; the merge note below recomputes
# every total.
#
# **This merge combined two independent placements**: D-080's single row, placed on main
# against main's table, and D-082's seventeen, placed on this branch against the table as
# this branch found it. It takes the union of both sides' rows and keeps every row in the
# shard its own side placed it in, re-placing none of them, and every total below is
# recomputed from the rows beneath it rather than carried over from either side. That
# recomputation also corrects three headers this branch carried that its own rows did not
# support — shards 3, 4 and 6, each out by 0.1 from the two-decimal `node` figures. The
# merged shards run 1328.1 to 1442.9 cpu s against a mean of 1370.1, so the night is
# bounded by shard 2 at about 5 % over the ideal. The next row added goes into the
# lightest, which is shard 4.
#
# D-083's three rows are the node's snapshot wiring, weighed on its own tree in release,
# run alone, as user+sys of the test binary. They are small and they stay small: that
# binary does not read ANANKE_SEEDS, because its situation is reached by construction on
# every seed rather than searched for, so it runs the same eight seeds at every tier.
# Putting it under the tiers is owed and is recorded as owed in D-083; the machine these
# three were measured on was not idle, which for figures of a few CPU seconds against
# shards of thirteen hundred changes no assignment. They were re-weighed after the
# review of #107, which raised the binary's seed count from eight to thirty-two and
# added the state read-back.
#
# **This merge combined two independent placements**: D-083's three `install` rows,
# placed on this branch against the table as it stood before the base moved, and the
# base's table (`phase-3-stage-b-sweeps`), which by then carried D-077's two `ranges`
# rows, D-080's one `raft` row and D-082's seventeen `node` rows re-weighed to two
# decimals by its review. It takes the union of both sides' rows and keeps every row in
# the shard its own side placed it in, re-placing none of them; the eleven older `node`
# rows this branch carried are the base's re-weighed seventeen, not rows of its own, so
# the base's figures and placements stand for them. Every total below is recomputed from
# the rows beneath it rather than carried over from either side.
# PROPOSED D-089 adds one row: the two seeds the aimed stream arms reach that go past the
# timer bound while a live install completes. It is pinned at two seeds, not swept, so the
# tier does not move it: weighed on its own tree from the built binary
# (`node-<hash> <test> --exact`) rather than through cargo, so the figure is the test's own
# cost and not a build's, at 0.65 user + 0.03 sys = 0.7 cpu s, load average 55.53/85.71/105.47
# on an Apple M2 of 8 cores on AC. Shard 4 was the lightest at 1372.3 and takes it, to 1373.0.
# Nothing else in the table moves: this slice adds no sweep and renames no test, and the two
# `node` sweeps whose figures it changes — the correct node's and the install variant's —
# keep their rows, their rates having moved and not their shapes.
# D-086's four rows are Phase 2's four stream variants on the node, weighed on its own
# tree at ANANKE_SEEDS=1000 in release, from the built binary (`node-<hash> <test>
# --exact`) rather than through cargo, so each figure is the test's own cost and not a
# build's. Apple M2, 8 cores, on AC Power, load average 7.96/11.11/16.52 with other
# agents' slices building beside it. Two run the whole tier because what they measure is
# a rate over it (261.0 and 265.8 cpu s); two run `high_rate_share()` because what they
# assert is a structural equality and an absence rather than a rate (43.4 and 73.9).
# Placed longest-first into the lightest shard, which took them to 6, 3, 4 and 5: the
# shards now run 1376.3 to 1567.6 cpu s against 1301.8 to 1440.4 before, so the night is
# bounded by shard 6 at about 6 % over the ideal. The next row added goes into the
# lightest, which is shard 4. On `scripts/premerge.sh` the four add 644.1 cpu s, about
# 80 s of wall on eight cores, which puts it near 455 s against the 374.64 s D-052
# recorded — inside the quarter of an hour D-040 sets (Q39).
#
# **D-086's rows are known LOW and a re-weigh on a quiet machine is owed.** The review
# round changed two of the four tests materially — the shared-directory variant now
# expresses its bug and wedges 30 % of seeds, where a wedged run plays out its whole
# length, and three of the four moved to `high_rate_share()` — and the re-review then
# measured what they actually cost:
#   a_leader_that_shares_one_snapshot_directory_is_caught_on_the_node  438.2 cpu s
#                                                        against its row of 265.8 (+65 %)
#   every_seed_passes_on_the_correct_node_under_the_raft_sweeps_arms  about 244 cpu s
#                                                        against its row of 163.5 (+49 %)
# Shard 6 is already the heaviest at 1 567.6, so about +172 puts it near **1 740**, some
# 19 % over the ideal rather than the 6 % this file's placement note claims above.
#
# The rows are **not** re-placed on those figures and the weights below are **not**
# changed, deliberately. Re-weighing here was attempted and abandoned at load average
# 177 with several agents' slices building, and a figure taken there is exactly the
# incomparable kind D-070 exists to stop — the same reason D-083 left its own tiering
# owed. The owner takes the re-weigh when the machine is quiet, and the shards are
# rebalanced then. Until that happens, read every D-086 row as a floor and shard 6's
# header as understated.
#
# **This merge combined two independent placements**: this branch's four `node` rows for
# PROPOSED D-086's stream variants, placed against the table as this branch found it, and
# the base `phase-3-stage-b-wiring`'s table, which carries D-077's and D-080's rows and
# D-082's seventeen `node` rows re-weighed to two decimals by its review. It takes the
# union of both sides' rows and keeps every row in the shard its own side placed it in,
# re-placing none of them; every total below is recomputed from the rows beneath it
# rather than carried over from either side, and the rows are grouped under the header
# that summarises them, which no earlier table had done.
#
# **Four rows of the union are gone, and they are the only ones.** The base named six of
# Phase 2's variants with a `*_is_not_re_asserted_on_the_node_yet` test each, so the
# table would carry the debt rather than a paragraph of prose; this branch reaches the
# stream path and re-asserts four of those six as tests of their own, with the rates they
# were measured at. The base's own comment says what should then happen — "the day the
# wiring lands, either the variant starts being caught and this test fails, asking to be
# turned into the assertion" — so the four superseded tests go and their rows go with
# them, or `check-nightly-shards.sh` fails on a table naming tests that do not exist.
# `AdoptionAsBuilt` and `RefusalNotDurable` wait on PR #86 and keep theirs.
#
# The merged shards run **1372.3 to 1604.9 cpu s** against a mean of 1467.5, so the
# night is bounded by shard 6 at about 9 % over the ideal — against the 6 % this
# branch's own placement note above claims, which was true of the table that note was
# written against and is not true of this one. The next row added goes into the
# lightest, which is **shard 4**.
#
# The D-086 rows below are still the floors the note above says they are, and the
# re-weigh on a quiet machine is still owed: this merge recomputes the headers from the
# rows, it does not re-measure the rows. Shard 6 carries both the largest D-086 row and
# the largest known understatement of one, so it is the shard that re-weigh moves.
#
# shard	binary	cpu_s	test
# shard 1: 1428.0 cpu s
1	engine	625.1	an_install_in_two_switches_is_caught
1	engine	36.8	an_install_whose_repair_precedes_its_switch_is_caught
1	engine	0.6	seed_44_never_opens_empty_in_either_mode
1	engine	0.4	same_seed_gives_byte_identical_trace
1	engine	0.3	seed_420_which_the_first_nightly_found_stays_green
1	raft	258.5	a_leader_that_ignores_incarnations_never_forgets
1	raft	208.6	a_server_that_installs_without_current_last_is_caught
1	raft	103.3	on_the_sweeps_disk_the_install_silence_deposes_the_leader_under_either_count
1	raft	14.6	a_follower_that_truncates_on_every_append_is_caught
1	raft	0.3	seed_1885_which_the_nightly_failed_on_a_trace_timestamp_no_longer_straddles_an_isolation
1	raft	0.3	seed_6325_which_the_nightly_found_stays_green
1	raft	0.2	seed_164_which_a_local_ten_thousand_seed_run_found_stays_green
1	raft	0.2	seed_5909_which_the_nightly_found_stays_green
1	wal	29.3	a_log_that_skips_the_checksum_is_caught
1	node	149.49	a_leader_that_trusts_the_clock_is_caught_on_the_node
# shard 2: 1442.9 cpu s
2	echo	0.2	different_seeds_give_different_traces
2	engine	581.7	the_live_install_crash_test_passes_every_seed
2	engine	211.2	a_compaction_that_deletes_its_inputs_before_the_manifest_is_caught
2	engine	34.6	a_checkpoint_that_copies_each_span_at_its_own_version_is_caught
2	engine	16.6	the_correct_engine_passes_every_seed_with_deep_levels
2	raft	276.0	a_leader_that_shares_one_snapshot_directory_and_streams_one_follower_at_a_time_is_caught
2	raft	151.5	a_leader_that_counts_nothing_from_a_refused_follower_is_caught
2	raft	4.4	a_server_that_sends_before_it_persists_is_caught
2	ranges	0.2	a_ranges_replicas_are_created_with_one_descriptor_and_created_once
2	ranges	0.1	a_node_holds_four_ranges_from_its_configuration_and_every_one_of_them_runs
2	ranges	0.1	a_seed_replays_to_the_same_trace
2	ranges	0.1	every_frame_between_two_nodes_may_carry_several_ranges
2	ranges	0.1	no_batch_frame_of_the_node_parses_as_a_frame_of_the_one_group_server
2	ranges	0.1	the_ranges_of_one_node_draw_their_own_election_timeouts
2	ranges	0.1	a_loss_in_the_shared_engine_refuses_the_whole_node_and_reseeds_beside_it
2	ranges	0.1	a_restarted_node_opens_the_directory_its_reseed_built_and_is_not_refused_again
2	node	165.78	every_seed_passes_on_the_correct_node_under_the_raft_sweeps_arms
# shard 3: 1562.0 cpu s
3	echo	0.2	trace_hash_matches_the_pinned_golden
3	engine	417.5	the_range_delete_crash_test_passes_every_seed
3	engine	235.9	the_seek_crash_test_passes_every_seed
3	engine	58.7	an_install_that_keeps_its_sources_numbers_is_caught
3	engine	52.4	a_span_checkpoint_without_syncs_is_caught
3	engine	39.8	an_install_whose_repair_follows_its_switch_is_caught
3	engine	0.3	seed_3123_which_the_nightly_found_supersedes_a_resurrected_segment
3	raft	281.7	a_server_whose_refusal_is_not_durable_is_caught
3	raft	158.5	a_leader_that_counts_a_refused_followers_rejections_whatever_its_stream_does_is_caught
3	raft	12.8	the_incremental_checker_agrees_with_the_fold_over_the_whole_trace
3	raft	0.7	seed_132_which_pinned_the_combined_variant_before_the_layout_reaches_no_wedge
3	raft	0.5	the_seed_42_trace_is_written_for_the_studio
3	raft	0.4	seed_5909_passes_under_both_bugs_together_which_is_the_finding
3	raft	0.3	seed_687_which_the_premerge_found_stays_green
3	raft	0.2	seed_385_which_a_local_ten_thousand_seed_run_found_stays_green
3	raft	0.2	the_membership_scenario_has_byte_identical_traces_for_one_seed
3	wal	21.2	a_log_that_acknowledges_before_syncing_is_caught
3	node	13.25	a_server_without_pre_vote_is_caught_on_the_node
3	node	4.03	a_server_that_applies_before_commit_is_caught_on_the_node
3	install	2.4	every_wiring_variant_is_caught
3	node	261.0	a_server_that_installs_without_current_last_is_injected_on_the_node
# shard 4: 1373.0 cpu s
4	echo	0.1	same_seed_gives_byte_identical_trace
4	engine	210.8	the_correct_engine_passes_every_seed
4	engine	38.6	an_install_that_switches_one_span_at_a_time_is_caught
4	parallel	6.4	a_seed_inside_the_driver_gives_the_sequential_trace
4	raft	355.6	a_leader_that_trusts_the_clock_is_caught_and_the_guard_revokes
4	raft	282.6	a_server_without_pre_vote_is_caught_on_the_term_raise_schedule
4	raft	237.7	a_server_that_resets_its_timer_on_any_message_is_caught
4	raft	107.1	a_server_that_counts_one_majority_in_joint_consensus_is_caught
4	raft	3.7	the_nightlys_eleven_variant_catches_of_the_trace_timestamp_gap_are_not_catches
4	raft	0.3	seed_119_which_pinned_the_refusal_that_is_not_durable_before_the_layout_refuses_nothing
4	raft	0.2	seed_2023_which_the_nightly_failed_on_a_trace_timestamp_no_longer_straddles_an_isolation
4	wal	36.9	the_correct_log_passes_every_seed
4	wal	0.2	the_seed_42_trace_is_written_for_the_studio
4	node	14.92	a_server_whose_refusal_is_not_durable_is_not_re_asserted_on_the_node_yet
4	node	2.32	a_follower_that_truncates_on_every_append_is_caught_on_the_node
4	node	0.52	a_server_that_sends_before_it_persists_is_caught_on_the_node
4	node	0.24	a_seed_replays_to_the_same_trace_on_the_node
4	node	0.11	a_node_under_the_raft_sweeps_arms_runs_every_range
4	node	0.1	the_nodes_arms_aim_at_every_range_and_not_at_one
4	node	73.9	the_pair_on_the_node_is_the_stream_half_alone_until_the_reseed_lands
4	node	0.7	seeds_272_and_516_go_past_the_timer_bound_while_a_live_install_completes
# shard 5: 1395.1 cpu s
5	echo	6.0	every_seed_satisfies_the_invariants_and_the_sweep_exercises_the_disk_faults
5	engine	19.5	a_seek_that_counts_tombstones_is_caught
5	raft	332.9	a_term_change_stepped_inside_an_isolation_from_a_message_received_before_it_is_excused
5	raft	304.8	the_correct_leader_steps_down_on_a_blocked_reseed_and_commits_through_an_open_one
5	raft	252.2	a_server_without_pre_vote_is_caught
5	raft	187.0	a_leader_that_commits_an_older_terms_entry_by_count_is_caught
5	raft	139.6	the_correct_server_passes_the_membership_scenario_on_every_seed
5	raft	50.7	a_server_that_applies_before_commit_compacts_past_its_commit_index
5	ranges	59.0	every_seed_passes_on_the_correct_node
5	node	43.4	a_leader_that_ignores_incarnations_has_no_incarnation_to_ignore_on_the_node_yet
# shard 6: 1604.9 cpu s
6	echo	0.1	a_busy_looping_task_fails_the_scenario_instead_of_hanging
6	engine	310.3	an_engine_that_acknowledges_before_the_log_is_caught
6	engine	204.5	an_engine_that_releases_a_memtable_before_the_manifest_is_caught
6	engine	39.2	a_range_delete_that_skips_the_memtables_is_caught
6	engine	0.3	the_seed_42_trace_is_written_for_the_studio
6	raft	311.9	the_correct_server_passes_every_seed
6	raft	266.4	a_server_whose_adoption_is_as_built_is_caught
6	raft	62.5	a_server_that_applies_before_commit_is_caught
6	raft	8.7	the_nightlies_removed_catches_meet_the_sweeps_assertions
6	raft	0.7	seed_2605_which_the_nightly_found_is_an_adoption_window_and_still_catches_the_variant
6	raft	0.7	seed_680_which_pinned_the_combined_variant_before_d056_no_longer_wedges
6	raft	0.6	same_seed_gives_byte_identical_trace
6	raft	0.4	a_replica_that_never_compacts_outgrows_the_follower_log_bound
6	raft	0.4	seed_158_which_pinned_the_refusal_that_is_not_durable_before_the_read_moved_loses_nothing
6	raft	0.4	seed_1_of_the_term_raise_schedule_now_straddles_its_isolations_by_decision_time
6	raft	0.3	seeds_3085_and_4065_which_the_nightly_found_linearize_inside_the_budget
6	raft	0.2	seed_102_pins_the_refusal_that_is_not_durable_which_a_hundred_seeds_can_miss
6	raft	0.2	seed_4_of_the_term_raise_schedule_steps_a_message_received_before_its_isolation
6	raft	0.2	seed_7381_which_the_first_nightly_found_stays_green
6	ranges	53.9	a_node_that_steps_a_core_while_its_persist_is_outstanding_is_caught
6	wal	34.4	a_log_that_skips_sync_dir_on_rotation_is_caught
6	wal	0.3	same_seed_gives_byte_identical_trace
6	node	14.65	a_server_whose_adoption_is_as_built_is_not_re_asserted_on_the_node_yet
6	node	13.12	a_server_that_resets_its_timer_on_any_message_is_caught_on_the_node
6	node	8.37	a_leader_that_commits_an_older_terms_entry_by_count_is_caught_on_the_node
6	install	6.3	the_correct_node_installs_on_every_seed
6	install	0.1	a_stream_flows_and_an_install_completes_on_every_range_and_every_follower
6	node	265.8	a_leader_that_shares_one_snapshot_directory_is_caught_on_the_node
